---
- name: Create an empty file on the destination
  hosts: test
  tasks:
    - name: Ensure the file exists
      ansible.builtin.file:
        path: /tmp/semaphore_test.txt
        state: touch
        mode: '0644'
